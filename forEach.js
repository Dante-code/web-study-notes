var a = ['a','b','c','d'];
a.forEach(function(value,index,obj){
    // console.log(value,index,obj);
    // 第一个参数是遍历的元素，第二个是索引号，第三个是遍历的数组
})
// JS还提供了一种方法叫做forEach(),这个方法只支持IE8以上的浏览器


var colors = ['red','green','yellow'];
colors.forEach(function(color){
    // console.log(color);
});

var b = {name:"can"};